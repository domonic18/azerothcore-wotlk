version: '3.8'

# 部署专用 docker-compose
# 使用外部 MySQL 数据库（测试/生产环境均已配置，无需初始化）
#
# 使用方法:
#   测试环境: docker-compose --env-file .env.test -f docker-compose.deploy.yml up -d
#   生产环境: docker-compose --env-file .env.prod -f docker-compose.deploy.yml up -d
#
# 数据库连接通过环境变量直接传递给 AzerothCore，不写入 .conf 文件。
# 环境变量名格式: AC_<CONFIG_NAME_UPPER>
# 在 .env 文件中直接填写完整的连接字符串即可。

services:
  ac-worldserver:
    image: ${REGISTRY}/${NAMESPACE}/azerothcore-server:${TAG:-master}
    container_name: ac-worldserver
    restart: unless-stopped
    tty: true
    stdin_open: true
    volumes:
      - ./data:/azerothcore/data
      - ./configs:/azerothcore/env/dist/etc
      - ./logs:/azerothcore/env/dist/logs
    ports:
      - "8085:8085"
    environment:
      - AC_DATA_DIR=/azerothcore/data
      - AC_LOGINDATABASEINFO
      - AC_WORLDDATABASEINFO
      - AC_CHARACTERDATABASEINFO
    command: ["./worldserver"]

  ac-authserver:
    image: ${REGISTRY}/${NAMESPACE}/azerothcore-server:${TAG:-master}
    container_name: ac-authserver
    restart: unless-stopped
    tty: true
    stdin_open: true
    volumes:
      - ./configs:/azerothcore/env/dist/etc
      - ./logs:/azerothcore/env/dist/logs
    ports:
      - "3724:3724"
    environment:
      - AC_LOGINDATABASEINFO
    command: ["./authserver"]
